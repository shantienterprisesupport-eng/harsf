# Copy to .env only on the machine/server running n8n.
# Never commit the real .env file or real credentials.
N8N_HOST=localhost
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/
GENERIC_TIMEZONE=Asia/Kolkata
N8N_ENCRYPTION_KEY=replace-with-a-long-random-secret-on-the-server
